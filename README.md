Simple method to graphically visualise the impact of hunting pressure relating to road and settlement proximity.

Set up jupyter env

pip install jupyter & ipykernel
python -m ipykernel install --user --name=.venv --display-name "Python (.venv)"